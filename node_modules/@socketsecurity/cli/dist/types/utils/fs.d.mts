export type FindUpOptions = {
    cwd?: string | undefined;
    onlyDirectories?: boolean | undefined;
    onlyFiles?: boolean | undefined;
    signal?: AbortSignal | undefined;
};
export declare function findUp(name: string | string[], options?: FindUpOptions | undefined): Promise<string | undefined>;
export declare function withTmpDir<T>(prefix: string, fn: (tmpDir: string) => Promise<T>): Promise<T>;
// Symlink-resolved absolute path when the target exists (e.g. macOS symlinks
// /tmp -> /private/tmp); falls back to a plain resolve so a not-yet-existing
// path still gets a usable absolute value instead of throwing.
export declare function realpathOrResolved(target: string): Promise<string>;
//# sourceMappingURL=fs.d.mts.map