import { aG as CalendarApiImpl, k as CalendarOptions } from './05b54543.js';

declare class Calendar extends CalendarApiImpl {
    el: HTMLElement;
    private baseId;
    private dataManager;
    private currentData;
    private renderRunner;
    private vdomRoot;
    private isRendering;
    private isRendered;
    private customContentRenderId;
    private currentClassName;
    private currentColorScheme;
    constructor(el: HTMLElement, optionOverrides?: CalendarOptions);
    private handleDataChange;
    private handleRenderRequest;
    render(): void;
    destroy(): void;
    batchRendering(func: any): void;
    pauseRendering(): void;
    resumeRendering(): void;
    resetOptions(optionOverrides: any, changedOptionNames?: string[]): void;
    private setClassName;
    private setHeight;
    private setColorScheme;
    private setIsRtl;
}

export { Calendar as C };
