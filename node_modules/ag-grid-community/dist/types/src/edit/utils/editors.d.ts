import type { BeanCollection } from '../../context/context';
import type { GetCellEditorInstancesParams, ICellEditor, ICellEditorValidationError } from '../../interfaces/iCellEditor';
import type { EditMap, EditValue } from '../../interfaces/iEditModelService';
import type { EditPosition } from '../../interfaces/iEditService';
import type { CellCtrl } from '../../rendering/cell/cellCtrl';
export declare const UNEDITED: unique symbol;
/** public api getCellEditorInstances */
export declare const getCellEditorInstances: <TData = any>(beans: BeanCollection, params?: GetCellEditorInstancesParams<TData>) => ICellEditor[];
export declare function _setupEditors(beans: BeanCollection, editingCells: Required<EditPosition>[], position?: Required<EditPosition>, key?: string | null, event?: Event | null, cellStartedEdit?: boolean | null): void;
export declare function _sourceAndPendingDiffer({ pendingValue, sourceValue, }: Pick<EditValue, 'pendingValue' | 'sourceValue'>): boolean;
/** Returns a copy of the edit map containing only entries where the pending value differs from the source value. */
export declare function _filterChangedEdits(edits: EditMap): EditMap;
export declare function _setupEditor(beans: BeanCollection, position: Required<EditPosition>, params?: {
    key?: string | null;
    event?: Event | null;
    cellStartedEdit?: boolean | null;
    silent?: boolean;
}): void;
export declare function _purgeUnchangedEdits(beans: BeanCollection, includeEditing?: boolean): void;
/**
 * Scoped purge for a single staged cell — O(1) instead of rescanning the whole edit map.
 * Staging one cell can only make that cell match its source, so bulk operations (paste/fill)
 * must purge per-cell here rather than calling {@link _purgeUnchangedEdits} per staged cell (O(N^2)).
 */
export declare function _purgeUnchangedEdit(beans: BeanCollection, position: Required<EditPosition>, includeEditing?: boolean): void;
export declare function _refreshEditorOnColDefChanged(beans: BeanCollection, cellCtrl: CellCtrl): void;
/**
 * Commits every open editor's buffered input (e.g. a Firefox date segment) so a stop validates and reads
 * the final value. Stop-only: the Firefox flush blurs the input, so it must never run per keystroke.
 */
export declare function _flushEditors(beans: BeanCollection): void;
/**
 * A popup editor closed — clicked away, Escape, or focus moved on. That is this cell's stop and no more:
 * the row or batch it belongs to keeps its own session, and block mode still holds an invalid value.
 */
export declare const _onPopupEditorClosed: (beans: BeanCollection, cellCtrl: CellCtrl, e?: MouseEvent | TouchEvent | KeyboardEvent) => void;
export declare const _syncFromEditors: (beans: BeanCollection, params: {
    persist: boolean;
    isCancelling?: boolean;
    isStopping?: boolean;
}) => void;
/** The single-cell half of {@link _syncFromEditors}: stages one open editor's value into the model. */
export declare const _syncFromEditorComp: (beans: BeanCollection, position: Required<EditPosition>, params: {
    persist: boolean;
    isCancelling?: boolean;
    isStopping?: boolean;
}) => void;
export declare function _syncFromEditor(beans: BeanCollection, position: Required<EditPosition>, editorValue?: any, _source?: string, valueSameAsSource?: boolean, params?: {
    persist?: boolean;
    isCancelling?: boolean;
    isStopping?: boolean;
}): void;
/** `edits` is explicit so that tearing down every open editor can never be the result of an omitted argument. */
export declare function _destroyEditors(beans: BeanCollection, edits: Required<EditPosition>[], params?: DestroyEditorParams): void;
export type DestroyEditorParams = {
    event?: Event | null;
    silent?: boolean;
    cancel?: boolean;
};
export declare function _destroyEditor(beans: BeanCollection, position: Required<EditPosition>, params: DestroyEditorParams, cellCtrl?: CellCtrl | undefined): void;
/** The uncached scan behind {@link EditService.editorsRequireValidation}, which owns the memo. */
export declare function _scanEditorsForValidation(beans: BeanCollection): boolean;
export declare function _populateModelValidationErrors(beans: BeanCollection, force?: boolean): void;
/** The row half of {@link _populateModelValidationErrors}: cell errors, and the editors behind them, untouched.
 *  Row rules only ever apply to a full row, so any other edit type leaves the map as it is — empty. */
export declare const _populateRowValidationErrors: (beans: BeanCollection) => void;
export declare function _validateEdit(beans: BeanCollection): ICellEditorValidationError[] | null;
/** The read half of {@link _validateEdit}: reports the validation state as it stands, changing nothing. */
export declare function _readEditValidationErrors(beans: BeanCollection): ICellEditorValidationError[] | null;
